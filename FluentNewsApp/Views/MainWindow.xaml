<Window x:Class="FluentNewsApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FluentNewsApp.Views"
        mc:Ignorable="d"
        Title="Fluent News App" Height="800" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Latest news" 
                   FontSize="24" 
                   HorizontalAlignment="Center" 
                   Margin="10"/>
        <Button Grid.Row="0" 
                Grid.Column="1" 
                Content="Refresh" 
                Margin="10" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Center"
                Command="{Binding RefreshCommand}"/>

        <local:CategoryControl Grid.Row="1" Grid.ColumnSpan="2" Category="{Binding Path=TechnologyNews.Category}" Articles="{Binding Path=TechnologyNews.Articles}"/>
        <local:CategoryControl Grid.Row="2" Grid.ColumnSpan="2" Category="{Binding Path=HealthNews.Category}" Articles="{Binding Path=HealthNews.Articles}"/>
        <local:CategoryControl Grid.Row="3" Grid.ColumnSpan="2" Category="{Binding Path=EntertainmentNews.Category}" Articles="{Binding Path=EntertainmentNews.Articles}"/>
    </Grid>
</Window>
