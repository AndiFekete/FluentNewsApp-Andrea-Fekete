<Window x:Class="FluentNewsApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:FluentNewsApp.Views"
        mc:Ignorable="d"
        Title="Fluent News App" MinHeight="500" MinWidth="300" Height="600" Width="500">
    
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction
                CommandParameter="{Binding}"
                Command="{Binding RefreshCommand}"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" 
                   Grid.ColumnSpan="2"
                   Text="Latest news" 
                   FontSize="24" 
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontWeight="Bold"
                   Margin="10"/>
        <Button Grid.Row="0" 
                Grid.Column="1" 
                Content="&#xE72C;"
                FontFamily="Segoe MDL2 Assets"
                FontSize="24"
                Margin="10" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Center"
                Command="{Binding RefreshCommand}"
                Foreground="{DynamicResource {x:Static SystemColors.AccentColorBrushKey}}"/>

        <local:CategoryControl Grid.Row="1" Grid.ColumnSpan="2" Margin="8" Category="{Binding Path=TechnologyNews.Category}" Articles="{Binding Path=TechnologyNews.Articles}" HasError="{Binding Path=TechnologyNews.HasError}" IsLoading="{Binding Path=TechnologyNews.IsLoading}"/>
        <local:CategoryControl Grid.Row="2" Grid.ColumnSpan="2" Margin="8" Category="{Binding Path=HealthNews.Category}" Articles="{Binding Path=HealthNews.Articles}" HasError="{Binding Path=HealthNews.HasError}" IsLoading="{Binding Path=HealthNews.IsLoading}"/>
        <local:CategoryControl Grid.Row="3" Grid.ColumnSpan="2" Margin="8" Category="{Binding Path=EntertainmentNews.Category}" Articles="{Binding Path=EntertainmentNews.Articles}" HasError="{Binding Path=EntertainmentNews.HasError}" IsLoading="{Binding Path=EntertainmentNews.IsLoading}"/>
    </Grid>
</Window>
